[tool.poetry]
name = "lovepy"
version = "0.1.0"
description = ""
authors = ["Noid"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
customtkinter = "^5.0.4"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
